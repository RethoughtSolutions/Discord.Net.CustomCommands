namespace Discord.Net.CustomCommands
{
    public class CommandInputResolver : IInputResolver
    {
        // TODO
        public ResolvedInput Resolve(string input)
        {
            //var firstWord = input.Split(' ').First();
            //var parameters;

            //return new ResolvedInput(firstWord);

            return null;
        }
    }
}